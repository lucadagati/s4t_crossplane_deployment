apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: crossbar-data
  name: crossbar-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
