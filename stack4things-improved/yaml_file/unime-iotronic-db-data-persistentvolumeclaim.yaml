apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: unime-iotronic-db-data
  name: unime-iotronic-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
