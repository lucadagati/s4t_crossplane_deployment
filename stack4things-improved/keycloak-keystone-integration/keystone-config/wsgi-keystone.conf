Listen 5000

<VirtualHost *:5000>
    ServerName keystone.keystone.svc.cluster.local

    # Logging Configuration
    LogLevel debug
    ErrorLog /var/log/apache2/keystone.log
    CustomLog /var/log/apache2/keystone_access.log combined

    # WSGI Configuration
    WSGIDaemonProcess keystone-public processes=5 threads=1 user=keystone group=keystone display-name=%{GROUP}
    WSGIProcessGroup keystone-public
    WSGIScriptAlias / /usr/bin/keystone-wsgi-public
    WSGIApplicationGroup %{GLOBAL}
    WSGIPassAuthorization On

    # Directory Permissions
    <Directory /usr/bin>
        <IfVersion >= 2.4>
            Require all granted
        </IfVersion>
        <IfVersion < 2.4>
            Order allow,deny
            Allow from all
        </IfVersion>
    </Directory>

    # OpenID Connect (OIDC) Configuration
    OIDCClaimPrefix "OIDC-"
    OIDCClaimDelimiter ;
    OIDCResponseType "code"
    OIDCPassClaimsAs both
    OIDCScope "openid email profile"
    OIDCProviderMetadataURL https://keycloak.keycloak.svc.cluster.local:8443/realms/stack4things/.well-known/openid-configuration
    OIDCSSLValidateServer Off

    # Proviamo la versione compatibile del comando
    OIDCRemoteUserClaim preferred_username

    OIDCClientID keystone
    OIDCClientSecret 6OWR6HQAqfWQ2AES8S3uSsW3O52Mw4RJ
    OIDCRedirectURI http://keystone.keystone.svc.cluster.local:5000/v3/auth/OS-FEDERATION/websso/mapped/redirect
    OIDCCryptoPassphrase password_lunga_e_casuale_per_sicurezza

    # Protected Location for WebSSO
    <Location /v3/auth/OS-FEDERATION/websso/mapped>
        AuthType openid-connect
        Require valid-user
        LogLevel debug
    </Location>
</VirtualHost>
